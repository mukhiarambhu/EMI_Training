import { ApplychangesDirective } from './applychanges.directive';

describe('ApplychangesDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplychangesDirective();
    expect(directive).toBeTruthy();
  });
});
